from routes/handler007
DEBUG  (:multipart-params "upload")

=========================================
For emacs ibuffer:

load-file ~/elisp/ibuffer-filter.el
M-x ibuffer
=========================================
